defmodule LiftskitBackend.Repo.Migrations.FixExercisePerformedTypeEnum do
  use Ecto.Migration

  def change do
    # Fix the exercise_performed type enum if needed
    # This migration was created to fix enum issues but appears to be empty
    # Adding a comment to indicate it's intentionally empty for now
  end
end
