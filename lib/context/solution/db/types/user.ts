// User-related types
export interface User {
  id: string;
  username: string;
  email: string;
  // Add any additional fields that your frontend expects
}

// Import ElixirUser from auth.ts
import { ElixirUser } from './auth';
